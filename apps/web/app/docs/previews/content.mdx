# Shared preview demos

Shared demo instances have been discontinued.

To try a module, use the pinned digest and compose snippet on each [module](/catalog) page and run it locally. That gives you the same verified image without a shared hosted demo.
